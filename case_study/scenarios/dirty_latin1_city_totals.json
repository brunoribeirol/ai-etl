{
  "id": "dirty_latin1_city_totals",
  "category": "adversarial_dirty_data",
  "description": "Sprint 22 fixture (Latin-1 encoded names/cities) - same shape as dirty_semicolon_city_totals, different real encoding bug already confirmed by Sprint 22.",
  "data": {
    "kind": "fixture",
    "path": "tests/fixtures/dirty_data/csv_latin1_encoding.csv"
  },
  "spec_template": "Read the file {csv_path}. Save the result to {output_path}.",
  "business_question": "Qual a soma de valor por cidade?",
  "expect_failure": false,
  "mock": {
    "transformations": [],
    "transform_code": "def transform(dfs: dict) -> pd.DataFrame:\n    return dfs[\"data\"]\n",
    "planner_response": [
      {"question": "Qual a soma de valor por cidade?", "type": "descriptive"}
    ],
    "gold_codes": [
      "valor_num = pd.to_numeric(df[\"valor\"].astype(str).str.replace(\",\", \".\", regex=False), errors=\"coerce\")\ngold_df = df.assign(valor_num=valor_num).groupby(\"cidade\")[\"valor_num\"].sum().reset_index()\nfig = go.Figure()\nnarrative = \"Soma de valor por cidade calculada a partir dos dados extraídos.\""
    ],
    "science_codes": [],
    "advisor_response": {
      "recommendations": [],
      "summary": "Totais por cidade calculados a partir de um arquivo com nomes acentuados em Latin-1."
    }
  }
}
